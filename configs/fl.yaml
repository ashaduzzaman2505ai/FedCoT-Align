experiment:
  current_mode: "fedcot" # Options: fedcot, centralized, standard_fl, fedrag
  baselines:
    enabled: true
    list: ["centralized_cot", "standard_fl"]

loss_weights:
  lambda_verifier: 0.5
  lambda_align: 1.0  # Set to 0.0 automatically when running standard_fl