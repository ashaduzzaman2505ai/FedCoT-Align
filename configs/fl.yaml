federated:
  num_rounds: 20
  num_clients: 10
  fraction_fit: 0.5       # Sample 5 clients per round
  local_epochs: 1
  batch_size: 8           # Optimized for T4
  strategy: "FedAvg"      # Core baseline

simulation:
  gpu_per_client: 0.2     # Allow 5 clients per T4 GPU